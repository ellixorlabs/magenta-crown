/**
 * Solid fill behind the loader monogram (`/public/branding/mc-loader-logo.webp` with PNG fallback;
 * sampled from the asset via ffmpeg).
 * Keep loader shell, segment splash, and nav chip aligned so the seam disappears.
 */
export const MC_LOADER_MAROON = "#7d1e3e";
